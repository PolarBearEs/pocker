[package]
name = "pocker-test-registry"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
hex = "0.4.3"
sha2 = "0.10.9"
tokio = { version = "1.48.0", features = ["io-util", "net", "rt", "sync", "time"] }
